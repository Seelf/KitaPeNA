from setuptools import setup

setup(
    name='mis_extensions',
    version='0.1',
    author='Dawid',
    description='Toolbox Extensions (Backend Logic)',
    setup_requires=['pybind11>=2.5.0'],
    install_requires=['pybind11>=2.5.0'],
)
